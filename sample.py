# -*- coding: utf-8 -*-
"""Thin CLI for TOPOTEX sampling — all logic lives in
topotex.pipelines.inference (flags unchanged)."""

from topotex.pipelines.inference import main

if __name__ == "__main__":
    main()
