# -*- coding: utf-8 -*-
"""Thin CLI for TOPOTEX evaluation — all logic lives in
topotex.pipelines.evaluation (protocol and flags unchanged)."""

from topotex.pipelines.evaluation import main

if __name__ == "__main__":
    main()
